#!/bin/sh
#
# Show number of docker containers running

COUNT=$(docker ps -q | wc -l | sed -r 's/^0$//g')

case $BLOCK_BUTTON in
	3) notify-send "<b> Containers:</b><br>lol" ;;
esac
        
echo "$COUNT"
#!/bin/bash
#
# Shows CPU temperature and CPU hogs with right click.

case $BLOCK_BUTTON in
	3) notify.sh "<b> CPU hogs:</b><br>$(ps axch -o cmd:15,%cpu --sort=-%cpu | head)" ;;
esac

sensors | awk '/temp1/ {print $2}'

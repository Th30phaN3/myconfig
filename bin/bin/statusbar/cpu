#!/bin/sh
#
# Shows CPU temperature and CPU hogs with left click.

case $BLOCK_BUTTON in
	3) mynote.sh "<b> CPU hogs</b>:<br>$(ps axch -o cmd:15,%cpu --sort=-%cpu | head)" ;;
esac

sensors | awk '/temp1/ {print $2}'
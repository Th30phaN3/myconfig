#!/bin/sh
#
# Show disk space. $1 should be drive mountpoint.

[ -z "$1" ] && exit

case $BLOCK_BUTTON in
	3) pgrep -x dunst >/dev/null && notify-send "$(df -h --output=target,used,size)" ;;
esac

printf " %s" "$(df -h "$1" | awk ' /[0-9]/ {print $3 "/" $2 " (" $5 ")" }')"

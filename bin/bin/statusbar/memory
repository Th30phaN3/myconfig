#!/bin/sh
#
# Show used RAM and memory hogs with left click.

case $BLOCK_BUTTON in
	3) notify-send "<b> Memory hogs:</b><br>$(ps axch -o cmd:15,%mem --sort=-%mem | head)" ;;
esac

free -h | awk '/^Mem:/ {print $3 "/" $2}'